#include<stdio.h>
/**
   * main - Entry point
   *
    * Return: Always 0 (Success)
     */
int main(void)
{
char ch = 'a';
for (ch = 'a'; ch <= 'z'; ch++)
{
putchar(ch);
}
char CH = 'A';
for ('A'; CH <= 'Z'; CH++)
putchar(CH);
putchar('\n');
return (0);
}
